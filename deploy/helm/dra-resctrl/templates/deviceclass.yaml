apiVersion: resource.k8s.io/v1
kind: DeviceClass
metadata:
  name: {{ include "dra-resctrl.fullname" . }}-slices
  labels:
    {{- include "dra-resctrl.labels" . | nindent 4 }}
spec:
  selectors:
    - cel:
        expression: "device.driver == '{{ .Values.driverName }}'"
